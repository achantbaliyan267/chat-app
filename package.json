{
  "name": "mern-chat-app",
  "version": "1.0.0",
  "description": "Fullstack MERN Realtime Chat",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  }
}
